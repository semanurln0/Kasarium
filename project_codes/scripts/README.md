# Phase 1 Data Pipeline (moved)

This folder contains the `scripts` package (moved under `project_codes/`) and
houses the Phase 1 data pipeline implementation used to generate CSV outputs
for database seeding.

Run the top-level runner `P1_database_check.py` from the repository root to
execute the pipeline.
