"""project_codes.scripts package (moved from repo root `scripts/`).

This package mirrors the original `scripts` package. The repository keeps a
tiny `scripts/__init__.py` at the repo root that points `__path__` here so
existing imports like `import scripts.data.run_phase1` continue to work.
"""

__all__ = []
